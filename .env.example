# OAuth application client IDs. Client IDs are public identifiers, not secrets.
# Both providers must use the matching redirect URI shown below.
KOMA_ANILIST_CLIENT_ID=
KOMA_MAL_CLIENT_ID=

# AniList redirect URI:       koma://oauth/anilist
# MyAnimeList redirect URI:   koma://oauth/myanimelist
